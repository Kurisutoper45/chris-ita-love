Chris & Ita — Mobile-first Pixel Love site (Vite React template)

How to run locally:
1. npm install
2. npm run dev -- --host   # to access from other devices on same LAN (e.g. your phone)
3. open the URL shown in terminal on your phone

This template includes:
- Mobile-first React component (src/components/MobileLoveSite.jsx)
- Tailwind CSS setup
- Framer Motion installed (for simple animations)

Replace placeholder images/video in the GALLERY array inside MobileLoveSite.jsx with your own URLs.

If you want a zip prepared with media hosting help, tell me.
